repos:
  - repo: local
    hooks:
      - id: whitebox-registry-validate
        name: validate registry.json (global primary key + governance)
        entry: python whitebox/registry/validate.py registry.json
        language: system
        files: ^registry\.json$
        pass_filenames: false
